node_modules
dist
.next
.turbo
coverage
# Vendored WebAssembly loaders copied in at build time (gitignored).
apps/web/public/vendor
pnpm-lock.yaml

# Markdown is hand-formatted here: the spec, ADRs, rules and review tables are read as
# documents, and Prettier's table alignment makes wide tables far less readable than the
# hand-wrapped originals. Formatting these is not a correctness concern.
*.md
