# Pay Sheet

Salary components by employee for the month.

## Where to find it

**TODO(review): R-07** — TallyPrime menu path to be verified and added.

## Settings that work best

- Period: the full month.
- Include employee name, earnings, deductions and net pay.

## Export

- Export to Excel (.xlsx) where you can; CSV, PDF, text and HTML exports also work.
- Export the report as shown; do not edit, re-sort or delete rows afterwards.
- One report per file is simplest; a workbook with several sheets is also read.

## What we handle for you

- Employee names, PAN, UAN and bank account numbers are tokenised before anything is sent to the AI.

## If a file is refused

- Any export format is accepted; files are encrypted when they arrive and kept for you until you delete them. Photos and scans are refused, because they have no text to read. Macros are never run.
- Very large files: split the period into smaller exports.
